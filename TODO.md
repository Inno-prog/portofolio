# TODO - Hero Section Updates

## Steps
- [x] Step 1: Dezoom background image further in `hero.css` (slowZoom keyframe scale from 1.150 to 0.80)
- [x] Step 2: Make text content occupy full horizontal width in `hero.html`
  - Removed max-width constraints (`max-w-3xl`, `max-w-2xl`, `max-w-xl`)
  - Sections now stretch horizontally with `w-full`
  - Middle and bottom sections use `md:flex-row md:justify-between` to spread across width
  - Increased padding and gaps for better horizontal spread

